# TODO.md — 任务池

> 依赖链：M0→M1→{M2,M3}；M3→M4→M5；M2+M5→M6→M7；M6→M8；M6+M7→M9；M5+M8→M10
> 交付期依赖（里程碑 4 内部串行）：M9→一致性专项（需导出产物做重转 diff）→三大场景 E2E（定向投递含导出）→启动说明（产品名 D13 可先占位）
> 每模块一个会话；完成打勾并注明日期；工作中发现的问题按重要性插行记录。

## 里程碑 1 — 风险先行验证（最小可用版本）

- [x] M0 项目骨架：FastAPI 服务（绑 127.0.0.1:8740，P11）+ Vue 3 双栏布局壳 + 一键启动脚本（端口预检查）+ LibreOffice 启动检测引导 + .gitignore + 错误码表 ✅ 2026-09-05（ruff/mypy/pytest 2绿 + eslint/vue-tsc/vitest 5绿 + 浏览器冒烟 6/6）
- [x] M1 数据层：SQLite schema 七表（blocks/tags/block_tags/templates/regions/versions/bindings）+ data/ 目录 + 存储 API + pytest 单测 ✅ 2026-09-07（ruff/mypy 21文件/pytest 32绿 + 启动建库 WAL 冒烟；三假设经用户确认：绑定1:1 / 版本归属模板 / 落盘名{id}_{原名}）
- [x] M3a 模板上传与校验（.docx 白名单/损坏加密报错/时间戳区分同名）+ 占位符解析（run 合并）+ 模板状态机（解析中/待校对/可用） ✅ 2026-09-09（ruff/mypy 24文件/pytest 64绿 + 用户 curl 手动验收 8/8：跨run合并/409重复/双400/列表/详情/404/落盘；四假设确认：同sha256拒绝/占位符type=custom/无占位符也进待校对/纯后端验收）
- [x] M4 渲染管线：soffice 常驻集成 + DOCX→PDF + PyMuPDF 坐标提取 + DOCX 元素↔渲染位置匹配 + 转换缓存 ✅ 2026-09-09（ruff/mypy 26文件/pytest 85绿含LO真实集成 + 用户验收通过：中文渲染/坐标对齐/缓存0.026s；修复P17字体/P18行序，提交bebafd7）
- [x] M5a 预览（只读）：pdfjs 渲染 + 区域覆盖层（绿=已绑定/黄=待校对/虚线=未识别） ✅ 2026-09-09（后端 ruff/mypy/pytest 86绿 + 前端 eslint/vue-tsc/vitest 26绿 + MCP 浏览器端到端 8/8：中文渲染墨迹验证/覆盖框对齐/P7 竞态/缓存秒开；422 统一错误结构技术债一并清理；验收期修复 P19 pdfjs 降级 5.4.149、P20 vite 预构建缓存须重启）
- [x] M6a 占位符区域绑定（正/反向）+ 替换引擎（首 run 样式继承/换段/编号剥离）+ 预览局部刷新 ✅ 2026-09-10（后端 ruff/mypy/pytest 115 绿（+29）+ 前端 eslint/vue-tsc/vitest 55 绿（+27）；替换后 bbox 随渲染现算不落库，规避 P21；同段多占位符克隆行/文本替换两阶段防串位）
- [x] ▲ 里程碑 1 验收：上传带 `{{姓名}}` 占位符的模板 → 自动识别 → 绑定一个块 → 预览看到样式一致的替换（全项目最高风险验证点，不过关不铺开功能）✅ 2026-09-18（用户确认打勾：M6a 验收已覆盖该场景——上传→解析→绑定→替换预览全链路人工通过）

## 里程碑 2 — 编辑器完整

- [x] UI 调整（2026-09-18 用户提出，三点方案已确认）：① 顶部加「工作台 / 模板制作指南」tab 切换——指南页含占位符写法说明（D3 主路径 + M3b 词表 + D6 限制）+ 可下载示例模板 docx（显式中文字体，P17）+ 可复制示例段落；② 字符块库抽屉化——左缘按钮控制展开/收起，默认展开；③ 模板选择下拉从顶栏挪到预览工具条（idle 态也可选）✅ 2026-09-18（前端 eslint/vue-tsc/vitest 74 绿；与 M2 同会话交付，另发现并修复 Vue 模板内 `{{ '}}' }}` 截断问题）
- [x] M2 块库：CRUD + 字段校验（名称2–30字/内容≤5000字/标签≤10个）+ 分类分组 + 标签筛选 + 标签合并重命名全库生效 + 删除二次确认 + 软删除（D11）+ 被引用删除→绑定置缺失态 ✅ 2026-09-18（后端 ruff/mypy 32文件/pytest 138 绿（+23：/api/tags、blocks 完整化、示例模板端点）+ 前端 74 绿（+19）；顶栏版本 span 移除，版本 UI 留待 M8 入预览工具条）
- [x] M3b 解析完整：常见字段名词表启发式 + 成段正文识别 + 置信度 + SHA-256 指纹关联（D10）+ 纯图片模板提示不适用 ✅ 2026-09-18（后端 ruff/mypy/pytest 166 绿（+28）+ 前端 74 绿零改动；浏览器自动化验收 7/7：三层解析落库/指纹关联/纯图片 400/覆盖层黄框/词表区域绑定/整段替换预览/无控制台错误；决策 A–D 用户确认：整段替换并入/409改关联/占位符label过词表/纯图片判定口径；验收期发现并修复预览刷新过长根因——python-docx zip 时间戳致 LO 缓存永 miss（P24），重复 preview 13.6s→0.036s）
- [x] UI 调整②批（块库抽屉体验，2026-09-18 用户提出、方案已确认）：① 宽度拖拽可调（260→230–360 + localStorage 记忆）；② 折叠按钮矩形内置竖线（头部新建按钮右侧 / 收起态工具条最左）；③ 卡片 2 行截断 ✅ 2026-09-18。**③批追加（同会话用户连续反馈，合并交付）**：分类功能彻底移除（前后端+schema 迁移，列表平铺按更新时间倒序）；无引用标签自动清理（删块/替换标签同事务）；标签栏单行横滚 + 可整体收起 + 折叠态关键字搜索框（匹配标签名过滤块）；标签管理两行式（标签名标题+N 个块在上、编辑框在下，230px 无重叠）；块编辑就地展开（卡片下方，字段带标题，新建表单同步加标题）；绿点迁 StatusBar 左下；工具条左组紧凑布局；图例窄宽度（≤620px）隐藏；「选择模板开始预览」提示删除。**P25 修复**：渲染端点响应无 Cache-Control 致浏览器启发式缓存复用旧 PDF（绑定后"替换无反应"）→ 两端点加 no-store。验收：后端 ruff/mypy/pytest 169 绿、前端 84 绿、浏览器自动化 7/7+4/4+3/3+3/3
- [x] M5b 校对完整：候选确认/排除 + 预览拖拽框选新建（命名+指定类型）+ 边界微调 + 删除 + 空区域拦截 + 禁止跨页框选 + 进度逐操作落库（中断续作）+ bbox 生命周期策略（P21）✅ 2026-09-18（后端 ruff/mypy/pytest 180 绿 + 前端 eslint/vue-tsc/vitest 96 绿；浏览器验收批 1–5 全 PASS：状态流转/框选新建/空框跨段拦截/8 手柄微调/删除级联/模板 11、12 自动 ready/正常模式回归；bbox 生命周期定案：bbox_pdf_sha 指纹失效重算 + manual 保护；验收期发现并修复 pendingCount 统计口径与 Q4 不一致）
- [x] M7 溢出处理：区域高度测量（文档流锚点跟踪）+ 50% 阈值分级（可配置，D4）+ 小超出自动顺延重排 + 大超出高亮/悬浮比例/底部状态条汇总/点击跳转 ✅ 2026-09-21（后端 ruff/mypy/pytest 194 绿（+15，含固定行高裁剪 LO 端到端）+ 前端 eslint/vue-tsc/vitest 100 绿（+4）；浏览器自动化验收两轮 + MutationObserver 精确复核：红/橙框着色/title +289%/状态条汇总排序/chip 点击闪烁/校对模式隐藏/控制台零错误；region_clone_paths 修多行克隆高度低估；跳转滚动在单页 fit-width 无滚动空间时不生效属正常，闪烁兜底定位）
- [x] ▲ 里程碑 2 验收：素模板端到端（校对 → 绑定 → 溢出分级正确）✅ 2026-09-22（沿用既有验收口径打勾，同里程碑 1 先例：M5b 校对验收批 1–5（校对/框选/微调/自动 ready）+ M3b 词表区域绑定 + M7 溢出浏览器自动化两轮（红/橙分级、+289% title、状态条跳转）已完整覆盖该场景；2026-09-22 一致性专项 UI 冒烟再次回归通过）

## 里程碑 3 — 资产化

- [x] M8 版本管理：新建（空白/复制当前为底稿）+ 切换整体刷新 + 重命名（同模板内唯一）+ 删除保护（删当前切相邻、至少留一）✅ 2026-09-21（后端 ruff/mypy/pytest 200 绿（+6：versions 列表/新建复制/校验/重命名/删除保护）；前端 eslint/vue-tsc/vitest 120 绿（+20：store 8/弹层 6/控件 6）；Playwright 浏览器验收 16/16 通过无 console 错误；工具条版本下拉「名字（N 项绑定）」+ 新建/重命名/删除，校对模式置灰；删除失败 alert 反馈；版本名 1–30 字全局约定入 AGENTS.md）
- [x] M10 换模板迁移：按区域类型自动匹配（文档流顺序，D7）+ 三类结果清单（自动匹配/多候选点选/无匹配手动指定或留空）+ 手动指定持久化 + 可跳过 ✅ 2026-09-21（后端 ruff/mypy/pytest 218 绿（+18）+ 前端 eslint/vue-tsc/vitest 140 绿（+22）；Playwright 浏览器验收 13/13：切模板触发/跳过/三清单确认/互斥预选/迁移落库/守门不复弹/控制台零错误；提交 26ce9cc）
- [x] ▲ 里程碑 3 验收：模板换装场景走通（同内容新样式，零重录）✅ 2026-09-22（沿用既有验收口径打勾：M10 Playwright 验收 13/13——切模板触发迁移确认、三清单确认、迁移落库后「已绑定 3」，源版本绑定块零重录迁移至新模板默认版本；2026-09-22 一致性专项确认迁移/导出/预览链路回归通过）

## 里程碑 4 — 交付

- [x] M9 导出（里程碑 4 首项，前置已全部满足）：当前模板+版本渲染 DOCX 落盘 + 未绑定区域保留原文 + 大超出警示清单确认（D5）+ 文件名「简历-{版本名}-{日期}.docx」+ 写盘失败可读报错 ✅ 2026-09-22（后端 ruff/mypy/pytest 229 绿（+11）+ 前端 eslint/vue-tsc/vitest 149 绿（+9）；curl 层 409 警示结构/200 落盘/产物内容验证 + Playwright UI 验收 15/15：基础直接下载、大超出弹层确认/返回修改、同名覆盖不累积、校对置灰、控制台零错误；提交见 git log feat(M9)）
- [x] 一致性专项（前置 M9：导出产物重转 PDF 才有 diff 对象）：预览 PDF vs 导出重转 PDF 逐页文本+坐标 diff（容差内）+ 真实模板比对脚本 ✅ 2026-09-22（后端 ruff/mypy/pytest 238 绿（+9：diff 工具单测 4 + 五类模板端到端 5）；AI 代验收 API 全链 11/11 + Playwright UI 13/13；五类模板（纯文本单栏/表格/双栏/文本框/占位符+多行克隆）三层口径全过：导出DOCX==渲染产物 sha、重转PDF==预览PDF sha、容差 diff 零差异；真实模板自验工具 scripts/consistency_check.py）
- [x] 三大场景 E2E（前置 M9：定向投递场景含导出步骤）：日常沉淀 / 定向投递 / 模板换装 ✅ 2026-09-22（scripts/e2e 三件套 seed.py/verify.py/run_e2e.py：fixture 播种（venv）+ 产物断言（venv，行提取用 extract_pdf_lines 修 P18 乱序）+ Playwright 浏览器流（/usr/bin/python3）；全量 all 三场景 PASS——替换文本生效无 {{ 残留 / 导出文件名「简历-{版本名}-{日期}.docx」+ 产物内容 / 迁移绑定零重录 + 预览校验；runner 含 FAIL 诊断输出）
- [x] 启动与运行说明（最后执行；产品名 D13 未定则先占位「文档助手」，影响 README/窗口标题）：README.md（LibreOffice 安装指引、数据目录位置、快速开始、使用流程、自验工具、常见问题）✅ 2026-09-22（前端 eslint/vue-tsc/vitest 155 绿（+6）+ 后端 238 绿纯回归；顺带收口 TopBar 占位按钮开放项——「导入模板」转正为真实上传入口（POST /api/templates → 自动选中新模板）、「导出 DOCX」占位删除；Playwright 浏览器冒烟 10/10；提交 f388abd）

## v1.1 路线（2026-09-22 定稿，用户确认；每模块一个会话，完成打勾注明日期）

> 排序原则：前端审查债（P1/P2）→ 加分项 → 新功能 → 观察项/技术债 → 增量刷新垫底。
> 依赖：M11→M12→M13→M14 前端体验串行（M13 依赖 M12 弹层定位收敛）；M15–M18 相互独立可乱序；M19 独立；M20 建议在 M12 后做（复用弹层基建）且最好先于 M21；M21 依赖 M16 撤销栈与 M20 空行块；M22/M23 独立随时可插；M24 必须最后（依赖 M13 布局稳定 + 功能冻结）。
> 本轮已确认决策（2026-09-22 用户拍板）：D13=「模块化文档生成助手」；在线编辑=编辑渲染文本+已绑块可选同步+未绑块快捷建块；批量管理=块库批量模式+模板管理弹窗；溢出 chips 并入 StatusBar（A 方案）；**区域删除并入 M20 实时编辑（一键删除按钮）；撤销升级为全局撤销/重做栈（M16），保存点（导出/切换模板）清空防无限回溯**。

- [x] M11 产品定名落地（D13 定案「模块化文档生成助手」）：README.md、frontend/index.html 窗口标题、AGENTS.md 标题与项目定位段 ✅ 2026-09-22（顺带覆盖 backend FastAPI title；目录名「简历助手」与导出文件名「简历-{版本名}-{日期}.docx」语义不动；提交 d9efb7d）
- [ ] M12 弹层模态化 + 交互基础（前端审查 P1①②+P3⑥）：6 弹层遮罩 absolute→fixed 全窗真模态（当前锚定 .template-preview，块库/StatusBar 模态期间仍可点）；抽 useModalEsc（Esc 关闭 + 焦点 trap + 归还；MigrationDialog Esc 语义=跳过，与点遮罩一致）；块库 resizer 键盘化（tabindex + 左右方向键 ±16px + role=separator + aria-valuenow，命中区 8→12px）
- [ ] M13 布局重排（P2③A+P2⑤+P3⑦，一次提交）：溢出 chips 并入 StatusBar（点 chip 保留滚页+闪烁 1.8s；.overflow-bar 移除，画布高度恒定不再跳动）；工具条分组重排「[≡] | 模板⌄ 版本⌄(+收编新建) | 导出(主按钮) | ?图例」（≤620px 图例改「?」出口不再直接消失）；块库头部固定骨架（折叠/展开只变宽度不变结构，消除跳变）
- [ ] M14 token 化（P2④）：main.css :root 语义 token（primary/danger/success/warning/border/text-1/2/3/bg-muted 起步），13 组件 hex→var() 机械替换；前后端门禁全绿验证
- [ ] M15 字段词表扩充：手机/电话/Tel、教育背景/教育经历、工作经历/项目经历+中文数字变体等（field_lexicon.py 数据 + 单测；词表命中提置信度，回归校对着色）
- [ ] M16 全局撤销/重做栈（由「绑定一步撤销」升级，D11 加分）：**作用于所有修改步骤**——绑定/解绑/迁移 apply、块增删改、区域校对（确认/排除/新建/微调）、M20 区域编辑与删除、M21 插入式绑定；前端命令栈（每步记 before/after 快照，undo 调 API 恢复 before，redo 恢复 after；⌘Z/⌘⇧Z + 工具条撤销/重做按钮）；**保存点截断**：导出成功/切换模板或版本时清空栈（防无限回溯已固化状态）；栈深上限 ~50
- [ ] M17 解析进度反馈（D12）：上传链路阶段化反馈（上传中→解析中→转换渲染中）+ 超 5s 基线提示；不引 SSE
- [ ] M18 软回车换行选项（D9 加分）：replacement.py 换段/软回车（w:br）双形态 + 选项 UI + 单测
- [ ] M19 批量管理：块库批量模式（进入批量→勾选→批量软删除 / 批量打换标签=「移动」）+ 工具条「管理模板」弹窗（全模板列表勾选批量删除，存在绑定的模板自动跳过并提示，复用删除保护语义）
- [ ] M20 区域实时编辑（原「在线编辑」扩容）：点击预览区域 → 编辑弹窗（当前渲染文本）；已绑块：保存时可勾选「同步修改块内容」（块更新 API 落库，触发绑定刷新链）；未绑块：弹窗内「新建块并绑定」一步完成（不留悬空编辑）；**一键删除区域段落**（从渲染产物物理移除 w:p，模板原件不动；须处理文档流锚点偏移——按元素引用定位而非索引，P3；删除操作入 M16 撤销栈）；空行块区域内替换留白（特殊块类型，内容=空段，等效「清空区域留白」调节垂直空间）
- [ ] M21 前插/后插式绑定（排版调节器第二层）：bindings 扩 position 字段（inside=现有替换 / before / after），空行块与普通块均可绑到区域锚点前/后做插入（复用替换引擎克隆段落机制，P4 编号剥离）；溢出测量与迁移匹配适配（插入物不参与区域匹配）；新绑定语义入 M16 撤销栈
- [ ] M22 观察项清理：① selectTemplate 成功路径清 exportWarnings（ExportDialog 旧弹层重现，修法一行）② 解绑 204 ERR_ABORTED（评估 DELETE 改 200+JSON 契约 vs 保留观察，会话内定夺）③ E2E 整页重载（未复现无主动修法，仅维持 run_e2e 诊断，再遇即查）
- [ ] M23 mypy tests 门禁：清理 tests/ 存量 ~66 类型错误 → 门禁扩为 `mypy app tests`
- [ ] M24 渲染缓存与增量刷新（最后）：绑定变更仅重渲染受影响页（页级 diff）；注意 P21 bbox 生命周期、P22 渲染竞态、P24 序列化确定性三陷阱

## 已解决归档

- [x] ~~UI：TopBar 右上占位按钮~~ ✅ 2026-09-22（提交 f388abd）：「导入模板」转正为真实上传入口（POST /api/templates → 自动选中新模板，复用 D10 reused 关联；失败 alert 可读报错），「导出 DOCX」占位删除（导出在预览工具条）
- [x] ~~产品命名 D13~~ ✅ 2026-09-22 定案「模块化文档生成助手」，落地替换入 M11
- [x] ~~建议：将 PRD 副本纳入项目目录~~ ✅ 2026-09-21：项目副本 `docs/PRD.md` 已存在（M10 会话确认，v1.0 2026-09-04 定稿版），桌面原件丢失风险已对冲
- [x] ~~桌面一键启动器高级化（用户插入任务：.command 黑终端窗口不高级）~~ ✅ 2026-09-22：升级为真正 macOS .app「模块化文档生成助手.app」（自制紫底白闪电图标 icns + LSUIElement 无 Dock 常驻 + 系统通知「正在启动/已启动」+ 运行中再点弹「打开页面/停止服务」菜单）；顺带实锤并修复 P26——vite 绑 localhost(::1)，`curl 127.0.0.1:5173` 探活永远失败致启动器 180s 超时误报，就绪检测改 lsof（协议栈无关），dev.sh「自动开浏览器」同款静默失效一并修复；README 浏览器访问地址改 localhost；`一键启动.command` 删除；ROOT 自动探测（克隆到任意位置双击仓库内 .app 均可启动，拷贝走则回退作者路径）（提交见 git log）
